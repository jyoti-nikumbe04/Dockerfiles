pipeline {
    agent any

    stages {
        stage('Run Docker Container') {
            steps {
                script {
                    def imageName = "zealous_wing:latest"
                    docker.image(imageName).inside {
                        // your docker run logic here
                        echo 'Running inside the container!'
                    }
                }
            }
        }
    }
}
